{
  "//": [
    "Points Phoenix at scripts/Start-LocalReleaseServer.ps1 instead of GitHub, and at an",
    "isolated installation root, so a developer can run the whole flow without publishing",
    "anything and without touching a real installation.",
    "",
    "    ./scripts/package.ps1 -Version 1.0.0 -OutputDirectory ./artifacts/1.0.0",
    "    ./scripts/Start-LocalReleaseServer.ps1",
    "    dotnet run --project src/Phoenix -- --environment Development --diagnostics --config config/examples/appsettings.local-testing.json",
    "",
    "Phoenix allows plain HTTP for loopback addresses only; everything else still needs HTTPS."
  ],

  "Phoenix": {
    "GitHub": {
      "Owner": "andikatjacobdennis",
      "Repository": "Phoenix",
      "ApiBaseUrl": "http://localhost:8099"
    },

    "Directories": {
      "Root": "%TEMP%\\phoenix-local\\{ProductSlug}"
    },

    "Updates": {
      "Channel": "Production",
      "Policy": "Automatic",
      "RemoteFallbackCount": 2
    },

    "Application": {
      "Url": "http://localhost:5080"
    },

    "Browser": {
      "LaunchOnSuccess": false
    },

    "Testing": {
      "//SimulateFault": [
        "One of: None, DownloadFailure, InterruptedDownload, ChecksumMismatch, CorruptArchive,",
        "StartupFailure, HealthCheckFailure, ActivationFailure.",
        "Only accepted when the environment is Development."
      ],
      "SimulateFault": "None",
      "SimulatePrerequisiteInstalls": true,
      "AllowBrowserLaunch": false
    }
  }
}
